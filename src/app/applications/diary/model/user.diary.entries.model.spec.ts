import { UserDiaryEntries } from './user.diary.entries.model';

describe('UserDiaryEntries', () => {
  it('should create an instance', () => {
    expect(new UserDiaryEntries()).toBeTruthy();
  });
});
